<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grad">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_real"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PassengerDriverInformActivity"></activity>
        <activity android:name=".DriverCallCheckActivity" />
        <activity android:name=".DriverCallListActivity" />
        <activity android:name=".PassengerCallActivity" />
        <activity android:name=".DriverLicenseActivity" />
        <activity android:name=".DriverSignInActivity" />
        <activity android:name=".PassengerSignInActivity" />
        <activity android:name=".FirstInstallActivity" />
        <activity android:name=".DriverLoginActivity" />
        <activity android:name=".PassengerLoginActivity" />
        <activity android:name=".SelectActivity" />
        <!-- Google map API 사용을 위한 코드 -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBMWOaafJ_sqHCTUAbMP6pSx708LssT-aY" /> <!--  -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>